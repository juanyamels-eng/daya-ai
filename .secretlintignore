**/node_modules/**
**/package-lock.json
**/pnpm-lock.yaml
**/yarn.lock
**/*.lock
**/coverage/**
**/test-results/**
**/*.map
screenshots/**
frontend/e2e/__screenshots__/**
**/.next/**

# Compose de desarrollo con credenciales locales de ejemplo
**/docker-compose.dev.yml

# Fixtures de tests con connection strings falsos (localhost)
**/src/__tests__/validateEnv.test.ts

# Plantilla backend/.env.example: valores placeholder. Se excluye con la
# directiva `# secretlint-disable` en el propio archivo (gitignore no ignora
# el dotfile .env.example).
